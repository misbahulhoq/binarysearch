## জাভাস্ক্রিপ্টে শর্ত (Conditions in JavaScript)

জাভাস্ক্রিপ্টে **শর্ত (Conditions)** ব্যবহার করা হয় নির্দিষ্ট কোন শর্ত সত্য (true) হলে এক ধরণের কাজ এবং মিথ্যা (false) হলে অন্য ধরণের কাজ করার জন্য। এটি প্রোগ্রামকে সিদ্ধান্ত নিতে সাহায্য করে।

---

### 🔹 **শর্তের ধরন**

জাভাস্ক্রিপ্টে সাধারণভাবে নিচের কয়েকটি শর্ত ব্যবহৃত হয়:

1. **if শর্ত**
2. **else শর্ত**
3. **else if শর্ত**
4. **ternary operator (?:)**

---

### 1️⃣ **if শর্ত**

যদি কোন শর্ত সত্য হয়, তাহলে নির্দিষ্ট কোড ব্লকটি চালানো হয়।

```javascript
let age = 18;

if (age >= 18) {
  console.log("আপনি ভোট দিতে পারবেন।");
}
```

### 2️⃣ **else শর্ত**

যদি if এর শর্ত মিথ্যা হয়, তাহলে else ব্লকটি চালানো হয়।

```javascript
let score = 75;

if (score >= 90) {
  console.log("আপনার গ্রেড: A");
} else if (score >= 70) {
  console.log("আপনার গ্রেড: B");
} else {
  console.log("আপনার গ্রেড: C বা এর নিচে");
}
```

### 3️⃣ **else if শর্ত**

```javascript
let score = 75;

if (score >= 90) {
  console.log("আপনার গ্রেড: A");
} else if (score >= 70) {
  console.log("আপনার গ্রেড: B");
} else {
  console.log("আপনার গ্রেড: C বা এর নিচে");
}
```

### 4️⃣ Ternary Operator ( ?: )

```javascript
let isMember = true;

let price = isMember ? 100 : 150;
console.log("মূল্য:", price);
```
