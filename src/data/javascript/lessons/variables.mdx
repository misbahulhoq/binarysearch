## জাভাস্ক্রিপ্টে ভেরিয়েবল তৈরি করা (`var`, `let`, `const`)

**ভেরিয়েবল (Variable)** হলো এমন একটি কন্টেইনার বা ধারক যেখানে ডাটা সংরক্ষণ করা যায়।

### 🔹 কেন ভেরিয়েবল দরকার?

- ডাটা সংরক্ষণ ও ব্যবহারের জন্য।
- পরবর্তী সময়ে ডাটা পরিবর্তন বা আপডেট করার জন্য ।
- প্রোগ্রামের বিভিন্ন স্থানে একই ডাটা ব্যবহারের সুবিধার্থে ।

### 🔹 জাভাস্ক্রিপ্টে ভেরিয়েবল ডিক্লেয়ার করার ৩টি উপায়:

### var (পুরাতন, কম ব্যবহার করা হয়)

```javascript
var name = "binarySearch";
console.log(name); // Output: binarySearch
```

### `let` (আধুনিক এবং বেশি ব্যবহৃত হয়)

```javascript
let name = "binarySearch";
console.log(name); // Output: binarySearch
```

### `const` (স্থায়ী মান সংরক্ষণের জন্য)

```javascript
const name = "binarySearch";
console.log(name); // Output: binarySearch
```
